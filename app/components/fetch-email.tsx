"use client"

//button that sends a get request for gmail subjects
//log into gmail via oAuth
//fetch and store client side as an object
//try and catch async axios call to chatgpt with json headers
//await response with 0 temperature model with a prompt that turns the json object into ITask
    {/*export interface ITask {
  id: string;
  text: string;
  isDone: boolean;
} */}

//

